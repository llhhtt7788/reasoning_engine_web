# API Configuration
# By default this points to the backend host. For local dev you can use the relative path
# and the Next.js rewrites (configured in next.config.ts) will proxy to http://localhost:11211.
# Example for dev: NEXT_PUBLIC_API_URL=/api/v1/chat/context
NEXT_PUBLIC_API_URL=/api/v1/chat/context

